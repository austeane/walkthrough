interface:
  display_name: "Walkthrough"
  short_description: "Generate focused walkthroughs from agent sessions"
  default_prompt: "Use $walkthrough to create a focused walkthrough of the agent work on this project"
policy:
  allow_implicit_invocation: false
